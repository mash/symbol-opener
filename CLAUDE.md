# Symbol Opener

VS Code / Cursor extension. Jump to symbol definitions via URI handler (for use with osc8wrap).

## Workflow

- Always start conversations in plan mode
- After plan is agreed, work in a worktree under `.worktrees/`
